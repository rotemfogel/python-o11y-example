# python-o11y-example

This repository is a simple python web application that manages users and passwords.
It allows creating a new user, querying users, validating authentication and removing users.
It exposes a REST API to allow above operations.
The stack uses FastAPI, PostgreSQL and OpenTelemetry.